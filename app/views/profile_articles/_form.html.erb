<%= form_with(model: profile_article, local: true) do |form| %>
  <% if profile_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profile_article.errors.count, "error") %> prohibited this profile_article from being saved:</h2>

      <ul>
        <% profile_article.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :article_id %>
    <%= form.text_field :article_id %>
  </div>

  <div class="field">
    <%= form.label :profile_id %>
    <%= form.text_field :profile_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
